# Escape sequence character in python.

# we can use as many escape sequence character as we want in one string.
# \n (backslash n) is a escape sequence character which we can use to break the line.
# \t is a escape sequence character which gives a tab space between words
a = "Ronaldo is the G.O.A.T\nRonaldo is the Mr.UCL\tRonaldo is top Goal Scorer" # \n meand new line.
print(a)

# "\" the language will not read the character after the "\" 
b = "the way through which we can use \"double quote\" in a string"
print(b)

# Can find more Escape sequence character and use them.